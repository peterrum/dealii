New: A serial triangulation owns now per default a MPI_COMM_SELF communicator. The 
communicator of an arbitrary (not just parallel) Triangulation class can now be 
queried via Triangulation::get_communicator() or DoFHandler::get_communicator().
<br>
(Peter Munch, 2021/02/28)
